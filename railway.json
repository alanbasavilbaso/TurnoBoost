{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "composer install --no-dev --optimize-autoloader && php bin/console asset-map:compile && php bin/console cache:clear --env=prod"
  },
  "deploy": {
    "startCommand": "php bin/console doctrine:migrations:migrate --no-interaction && php -S 0.0.0.0:$PORT -t public"
  }
}