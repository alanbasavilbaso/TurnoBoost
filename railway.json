{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "composer install --no-dev --optimize-autoloader && php bin/console asset-map:compile"
  },
  "deploy": {
    "startCommand": "php -S 0.0.0.0:$PORT -t public"
  }
}