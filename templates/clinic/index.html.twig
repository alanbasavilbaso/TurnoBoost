{% extends 'base.html.twig' %}

{% block title %}Mis Clínicas - TurnoBoost{% endblock %}

{% block body %}
<div class="container mt-4">
    <div class="row">
        <div class="col-12">
            <div class="d-flex justify-content-between align-items-center mb-4">
                <h1 class="h3">Mis Clínicas</h1>
                <a href="{{ path('clinic_new') }}" class="btn btn-primary">
                    <i class="fas fa-plus"></i> Nueva Clínica
                </a>
            </div>

            {% for message in app.flashes('success') %}
                <div class="alert alert-success alert-dismissible fade show" role="alert">
                    {{ message }}
                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                </div>
            {% endfor %}

            {% for message in app.flashes('error') %}
                <div class="alert alert-danger alert-dismissible fade show" role="alert">
                    {{ message }}
                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                </div>
            {% endfor %}

            {% if clinics is empty %}
                <div class="text-center py-5">
                    <div class="mb-4">
                        <i class="fas fa-hospital fa-4x text-muted"></i>
                    </div>
                    <h4 class="text-muted">No tienes clínicas registradas</h4>
                    <p class="text-muted mb-4">Crea tu primera clínica para comenzar a gestionar turnos</p>
                    <a href="{{ path('clinic_new') }}" class="btn btn-primary btn-lg">
                        <i class="fas fa-plus"></i> Crear Primera Clínica
                    </a>
                </div>
            {% else %}
                <div class="row">
                    {% for clinic in clinics %}
                        <div class="col-md-6 col-lg-4 mb-4">
                            <div class="card h-100 shadow-sm">
                                <div class="card-body">
                                    <h5 class="card-title">{{ clinic.name }}</h5>
                                    <p class="card-text text-muted">
                                        <i class="fas fa-map-marker-alt"></i> {{ clinic.address|slice(0, 50) }}{% if clinic.address|length > 50 %}...{% endif %}
                                    </p>
                                    <p class="card-text">
                                        <small class="text-muted">
                                            <i class="fas fa-phone"></i> {{ clinic.phone }}<br>
                                            <i class="fas fa-envelope"></i> {{ clinic.email }}
                                        </small>
                                    </p>
                                </div>
                                <div class="card-footer bg-transparent">
                                    <div class="btn-group w-100" role="group">
                                        <a href="{{ path('clinic_show', {'id': clinic.id}) }}" class="btn btn-outline-primary btn-sm">
                                            <i class="fas fa-eye"></i> Ver
                                        </a>
                                        <a href="{{ path('clinic_edit', {'id': clinic.id}) }}" class="btn btn-outline-secondary btn-sm">
                                            <i class="fas fa-edit"></i> Editar
                                        </a>
                                        <button type="button" class="btn btn-outline-danger btn-sm" data-bs-toggle="modal" data-bs-target="#deleteModal{{ clinic.id }}">
                                            <i class="fas fa-trash"></i> Eliminar
                                        </button>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <!-- Modal de confirmación de eliminación -->
                        <div class="modal fade" id="deleteModal{{ clinic.id }}" tabindex="-1">
                            <div class="modal-dialog">
                                <div class="modal-content">
                                    <div class="modal-header">
                                        <h5 class="modal-title">Confirmar Eliminación</h5>
                                        <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                                    </div>
                                    <div class="modal-body">
                                        <p>¿Estás seguro de que deseas eliminar la clínica <strong>{{ clinic.name }}</strong>?</p>
                                        <p class="text-danger"><small>Esta acción no se puede deshacer.</small></p>
                                    </div>
                                    <div class="modal-footer">
                                        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancelar</button>
                                        <form method="post" action="{{ path('clinic_delete', {'id': clinic.id}) }}" style="display: inline;">
                                            <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ clinic.id) }}">
                                            <button type="submit" class="btn btn-danger">Eliminar</button>
                                        </form>
                                    </div>
                                </div>
                            </div>
                        </div>
                    {% endfor %}
                </div>
            {% endif %}
        </div>
    </div>
</div>
{% endblock %}