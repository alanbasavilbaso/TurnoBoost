<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Página no encontrada - TurnoBoost</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        body { font-family: Arial, sans-serif; text-align: center; padding: 50px; }
        .error-container { max-width: 600px; margin: 0 auto; }
        h1 { color: #e74c3c; font-size: 3em; margin-bottom: 20px; }
        p { font-size: 1.2em; color: #666; margin-bottom: 30px; }
        .btn { background: #3498db; color: white; padding: 12px 24px; text-decoration: none; border-radius: 5px; }
    </style>
</head>
<body>
    <div class="error-container">
        <h1>404</h1>
        <h2>Página no encontrada</h2>
        <p>{{ message }}</p>
        <p>El dominio "<strong>{{ domain }}</strong>" no existe o no está disponible.</p>
    </div>
</body>
</html>